/*  ************************* ERROR MESSAGES ************************* */

/**
 * Stores custom error message
 *
 * @remarks
 * The enum contains all the custom error messages needed in our project.
 */
export enum MESSAGES {
  USER_NOT = 'Account not found',
  NOT_OWNER = 'Proof of ownership failed',
  PASSWORD_WRONG = 'Invalid password entered',
  ACCESS_DENIED = 'Access id denied',
  UNAUTHORIZED = 'Unauthorized',
  USERNAME_TAKEN = 'Username is taken',
  EMAIL_TAKEN = 'Email is taken',
  USER_TAKEN = 'User is taken',
}
